<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="styles.css">
    <title>Pagination</title>
</head>
<body>
    <% data.forEach(entry => { %>
       <h1><%= entry.title %></h1>
    <% }) %>
    <%if(page > 1){%>
        <a href="/pagination?page=<%=page-1%>">Before</a>
    <%}%>
    <% for(let i=iterator; i <= endingLink; i++) {%>
        <% if(i==page) {%>
            <a class="selectedLink" href="/pagination?page=<%=i%>"><%=i%></a>
            <%continue;%>
    <%}%>
         <a class="selectedLink" href="/pagination?page=<%=i%>"><%=i%></a>
         <%}%>
         <% if(page < numberofPages){ %> <a href="/pagination?page=<%=page+1%>">After</a><%}%>


</body>
</html>